{
  "name": "ai-video-guard",
  "version": "2.0.0",
  "description": "Anlamsal YouTube icerik filtresi (Firefox + Chrome)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:firefox": "node scripts/build.mjs --target=firefox",
    "build:chrome": "node scripts/build.mjs --target=chrome",
    "watch": "node scripts/build.mjs --watch",
    "test": "node --test test/*.test.js",
    "zip": "node scripts/build.mjs && node scripts/zip.mjs",
    "e2e": "node test/e2e/demo.mjs --mock",
    "e2e:firefox": "node test/e2e/firefox.mjs --mock",
    "e2e:kural": "node test/e2e/live-rule.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.49.1"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
